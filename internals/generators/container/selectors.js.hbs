import { createSelector } from 'reselect';

/**
 * Direct selector to the {{ camelCase name }} state domain
 */
export const get{{ properCase name }}Domain = () => state => state.get('{{ camelCase name }}');

/**
 * Other specific selectors
 */


/**
 * Default selector used by {{ properCase name }}
 */

export const get{{ properCase name }} = () => createSelector(
  get{{ properCase name }}Domain(),
  (substate) => substate.toJS()
);
export default get{{ properCase name }};
